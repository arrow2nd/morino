scripts:
  start: deno run --allow-net=:8000 --allow-read --allow-env --watch ./server.tsx
  build: deno run --allow-net --allow-read --allow-write --allow-env --allow-run --unstable ./tools/build.ts
  fmt: deno fmt .
